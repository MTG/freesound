install all dependencies as listed in the docs


installed numpy, psycopg2


local_settings.example.py -> local_settings.py

	change database info 
	- setting up prostgresql requires some postgres skills
	- create a new user (e.g. freesound, give it a password)
	- change pg_hba.conf to include a line to give freesound access
	- restart postgresql
	(N.B. DATABASE_HOST had to be set specifically to 'localhost', an empty string '' does not work)

	changed the MEDIAL_URL, port number was not standard (was 8080 instead of 8000)

	applied for RECAPTCHA_PRIVATE/PUBLIC_KEY
	
	set SECRET_KEY (to at least 16 characters)



extended urls.py for serving of media files (css/js/etc.)

	from django.conf import settings
	if settings.DEBUG:
	    urlpatterns += patterns('',
		(r'^%s/(?P<path>.*)$' % settings.MEDIA_URL.strip('/'), 'django.views.static.serve', 
			{'document_root': settings.MEDIA_ROOT, 'show_indexes': True}),
		(r'^fsmedia/(?P<path>.*)$', 'django.views.static.serve', {'document_root':
			settings.MEDIA_ROOT, 'show_indexes': True}),
	    )


run ./fake_smtp to be able to register
- N.B. sends emails with activation links like this: http://example.com/home/activate/xDWHdmNM1kD6IK6DNkvl3Q--/   (example.com!)



log in to the admin site and set the site url! (look for the 'sites' section)
- example.com is no good!



install solr
- apt-get install solr-tomcat (on ubuntu lucid)
- change SOLR url in local_settings.py (standard port for solr is 8080)
- copy the solr schema and config files to /etc/solr/conf/
- change solrconfig.xml to change the data directory to the ubuntu one: /var/lib/solr/data
