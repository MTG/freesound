{% import './icons.njk' as icons %}
{% macro stars(rating) %}
  {% for i in range(rating | round(0, 'floor')) %}
    {{ icons.icon('star', 'text-red')}}
  {% endfor %}
  {% if rating !== rating | round(0) %}
    {{ icons.icon('half-star', 'text-red')}}
  {% endif %}
  {% for i in range(5 - rating | round(0)) %}
    {{ icons.icon('star', 'text-light-grey')}}
  {% endfor %}
{% endmacro %}